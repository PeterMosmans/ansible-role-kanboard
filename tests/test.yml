---
- hosts: localhost
  remote_user: root
  become: true
  roles:
    # - PeterMosmans.apache2
    - ansible-role-kanboard
  vars:
    - apache2_php: true
    - kanboard_source: https://github.com/kanboard/kanboard/archive/v1.2.11.zip
